#paragma once
#include"character.h"
class Warrior: public Character {
public:
	Warrior();// : Character()
	//{
	//	manaPoints = this->strength;
	//}
	int heavyswingDmg=10;
	int damagedealt;
	int warriorHp = CharHp;
	int ragePoints;
	int hitPoints=19;
	//in comment write what is being called from where what is happening 
	int heavySwing() {
		//srand(time(0));//why it is commented what is it's function 
		//manaPoints=rand() % 100
		if (ragePoints > 15)
		{
		  ragePoints=ragePoints-15;
		  return heavyswingDmg*2;
		  //Multiply 
		}
		else
		  ragePoints=ragePoints+5;
	           return heavyswingDmg;
	};
	void displayWarriorInfo();

};
